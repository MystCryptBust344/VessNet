"""
src/datasets/__init__.py

Dataset loader modules will be added here in the next phase (model + training).
This directory is reserved for PyTorch Dataset classes for:
  - DRIVEDataset (source domain, supervised)
  - CHASEDataset (target domain, unsupervised/semi-supervised)
  - STAREDataset (external test, evaluation only)

Out of scope for the preprocessing phase.
"""
